<div class="col-sm-8 blog-main">
  <%= link_to 'Edit Blog', edit_blog_path(@blog) if logged_in?(:site_admin) %>

  <h2><%= @blog.title %></h2>

  <p><%= @blog.body %></p>

  <%= render 'comments/comment_form' %>

  <%= render @blog.comments %>
</div>

